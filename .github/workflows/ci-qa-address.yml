name: CI QA - Address Service

on:
  push:
    branches: [ "qa" ]
    paths:
      - 'users/address-service/**'
  pull_request:
    branches: [ "qa" ]
    paths:
      - 'users/address-service/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r users/address-service/requirements.txt

      - name: Build Docker image
        run: docker build -t address-service:qa users/address-service
